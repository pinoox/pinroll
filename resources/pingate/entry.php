<?php

/**
 * PinGate HTTP entry — generated at pinroll/pingate.php (upload to your host deploy root).
 */
declare(strict_types=1);

$configDir = __DIR__ . '/gate';
($run = require $configDir . '/bootstrap.php')($configDir);
